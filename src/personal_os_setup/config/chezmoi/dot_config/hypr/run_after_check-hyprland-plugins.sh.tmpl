{{- if eq .chezmoi.os "linux" -}}
#!/bin/bash
set -eu

# Plugin list/load logic lives in load-plugins.sh (shared with autostart.lua's hyprland.start hook)
exec "$HOME/.config/hypr/load-plugins.sh"
{{ end -}}
